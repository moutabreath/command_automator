from abc import ABC, abstractmethod

class SharedService(ABC):
    """Abstract base class for all shared services used in MCP"""
    
    def __init__(self):
        """Initialize the shared service"""
        pass